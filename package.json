{
  "name": "knowledge-management-system",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "build": "npm run build",
    "start": "npm run start",
    "lint": "npm run lint",
    "typecheck": "npm run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
